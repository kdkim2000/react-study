# 19. React & Next.js 종합 실습

## 1) 아키텍처 & 기능 흐름

* **Auth**: 로그인 시 `accessToken`(메모리 보관) + `refresh_token`(HttpOnly 쿠키, 서버만 접근).
* **API**: `/api/auth/*`, `/api/posts/*`, `/api/comments/*`, `/api/profile`
* **권한**: 본인 글/댓글만 수정·삭제 가능.
* **DB(Prisma + SQLite)**: `User`–`Profile`(1:1), `User`–`Post`(1\:N), `Post`–`Comment`(1\:N)

폴더:

```
fullstack-board/
├─ server/    # Express + Prisma + JWT
└─ client/    # Vite + React + TS + Tailwind
```

---

## 2) 전체 코드

### 2-1) 백엔드 — **server/**

#### a) 초기 설정

```bash
mkdir -p fullstack-board && cd fullstack-board
mkdir server && cd server
npm init -y
npm i express cors cookie-parser jsonwebtoken bcryptjs zod
npm i prisma @prisma/client
npx prisma init --datasource-provider sqlite
npm i -D typescript ts-node-dev @types/node @types/express @types/cors @types/cookie-parser @types/jsonwebtoken @types/bcryptjs
```

#### b) 파일: **server/tsconfig.json**

```json
{
  "compilerOptions": {
    "target": "ES2020",
    "module": "CommonJS",
    "strict": true,
    "esModuleInterop": true,
    "skipLibCheck": true,
    "outDir": "dist"
  },
  "include": ["src"]
}
```

#### c) 파일: **server/.env**

```
DATABASE_URL="file:./dev.db"
ACCESS_SECRET="dev_access_secret_change_me"
REFRESH_SECRET="dev_refresh_secret_change_me"
```

#### d) 파일: **server/prisma/schema.prisma**

```prisma
generator client {
  provider = "prisma-client-js"
}
datasource db {
  provider = "sqlite"           // 배포 시 postgresql 로 교체 가능
  url      = env("DATABASE_URL")
}

model User {
  id        Int       @id @default(autoincrement())
  email     String    @unique
  name      String
  password  String
  createdAt DateTime  @default(now())
  profile   Profile?
  posts     Post[]
  comments  Comment[]
}

model Profile {
  id        Int     @id @default(autoincrement())
  displayName String
  bio       String  @default("")
  user      User    @relation(fields: [userId], references: [id])
  userId    Int     @unique
}

model Post {
  id        Int       @id @default(autoincrement())
  title     String
  content   String
  createdAt DateTime   @default(now())
  updatedAt DateTime   @updatedAt
  author    User       @relation(fields: [authorId], references: [id])
  authorId  Int
  comments  Comment[]
  @@index([createdAt(sort: Desc)])
}

model Comment {
  id        Int      @id @default(autoincrement())
  content   String
  createdAt DateTime  @default(now())
  author    User      @relation(fields: [authorId], references: [id])
  authorId  Int
  post      Post      @relation(fields: [postId], references: [id])
  postId    Int
}
```

마이그레이션 & 클라이언트 생성:

```bash
npx prisma migrate dev --name init
npx prisma generate
```

#### e) 파일: **server/src/db.ts**

```ts
import { PrismaClient } from "@prisma/client";
const g = global as unknown as { prisma?: PrismaClient };
export const prisma = g.prisma ?? new PrismaClient({ log: ["error", "warn"] });
if (process.env.NODE_ENV !== "production") g.prisma = prisma;
```

#### f) 파일: **server/src/auth.ts** (JWT 유틸/미들웨어)

```ts
import jwt from "jsonwebtoken";
import type { Request, Response, NextFunction } from "express";

const ACCESS_SECRET = process.env.ACCESS_SECRET!;
const REFRESH_SECRET = process.env.REFRESH_SECRET!;

export function signAccess(userId: number) {
  return jwt.sign({ sub: userId }, ACCESS_SECRET, { expiresIn: "15m" });
}
export function signRefresh(userId: number) {
  return jwt.sign({ sub: userId, type: "refresh" }, REFRESH_SECRET, { expiresIn: "7d" });
}

export function authGuard(req: Request, res: Response, next: NextFunction) {
  const auth = req.headers.authorization || "";
  const token = auth.startsWith("Bearer ") ? auth.slice(7) : null;
  if (!token) return res.status(401).json({ error: "missing access token" });
  try {
    const payload = jwt.verify(token, ACCESS_SECRET) as { sub: number };
    (req as any).userId = payload.sub;
    next();
  } catch {
    return res.status(401).json({ error: "invalid/expired token" });
  }
}

export function verifyRefresh(rt: string) {
  const payload = jwt.verify(rt, REFRESH_SECRET) as { sub: number; type: string };
  if (payload.type !== "refresh") throw new Error("invalid");
  return payload.sub;
}
```

#### g) 파일: **server/src/validators.ts**

```ts
import { z } from "zod";

export const registerSchema = z.object({
  email: z.string().email(),
  name: z.string().min(2),
  password: z.string().min(4)
});
export const loginSchema = z.object({
  email: z.string().email(),
  password: z.string().min(4)
});
export const postCreateSchema = z.object({
  title: z.string().min(2),
  content: z.string().min(2)
});
export const postUpdateSchema = z.object({
  title: z.string().min(2).optional(),
  content: z.string().min(2).optional()
});
export const commentCreateSchema = z.object({
  content: z.string().min(1)
});
export const profileUpdateSchema = z.object({
  displayName: z.string().min(2),
  bio: z.string().max(500).optional().default("")
});
```

#### h) 파일: **server/src/routes.auth.ts**

```ts
import { Router } from "express";
import { prisma } from "./db";
import { registerSchema, loginSchema } from "./validators";
import bcrypt from "bcryptjs";
import { signAccess, signRefresh, verifyRefresh } from "./auth";

const r = Router();

r.post("/register", async (req, res) => {
  const parsed = registerSchema.safeParse(req.body);
  if (!parsed.success) return res.status(400).json(parsed.error.format());
  const { email, name, password } = parsed.data;
  const exists = await prisma.user.findUnique({ where: { email } });
  if (exists) return res.status(409).json({ error: "email exists" });
  const user = await prisma.user.create({
    data: {
      email, name, password: bcrypt.hashSync(password, 10),
      profile: { create: { displayName: name } }
    },
    select: { id: true, email: true, name: true }
  });
  const accessToken = signAccess(user.id);
  const refreshToken = signRefresh(user.id);
  res.cookie("refresh_token", refreshToken, {
    httpOnly: true, sameSite: "lax", secure: false, path: "/api/auth", maxAge: 7 * 24 * 60 * 60 * 1000
  });
  res.json({ accessToken, user });
});

r.post("/login", async (req, res) => {
  const parsed = loginSchema.safeParse(req.body);
  if (!parsed.success) return res.status(400).json(parsed.error.format());
  const { email, password } = parsed.data;
  const user = await prisma.user.findUnique({ where: { email } });
  if (!user || !bcrypt.compareSync(password, user.password))
    return res.status(401).json({ error: "invalid credentials" });
  const accessToken = signAccess(user.id);
  const refreshToken = signRefresh(user.id);
  res.cookie("refresh_token", refreshToken, {
    httpOnly: true, sameSite: "lax", secure: false, path: "/api/auth", maxAge: 7 * 24 * 60 * 60 * 1000
  });
  res.json({ accessToken, user: { id: user.id, email: user.email, name: user.name } });
});

r.post("/refresh", async (req, res) => {
  const rt = (req as any).cookies?.refresh_token;
  if (!rt) return res.status(401).json({ error: "missing refresh token" });
  try {
    const uid = verifyRefresh(rt);
    return res.json({ accessToken: signAccess(uid) });
  } catch {
    return res.status(401).json({ error: "invalid refresh token" });
  }
});

r.post("/logout", async (_req, res) => {
  res.clearCookie("refresh_token", { path: "/api/auth" });
  res.status(204).end();
});

export default r;
```

#### i) 파일: **server/src/routes.profile.ts**

```ts
import { Router } from "express";
import { prisma } from "./db";
import { authGuard } from "./auth";
import { profileUpdateSchema } from "./validators";

const r = Router();
r.use(authGuard);

r.get("/me", async (req, res) => {
  const userId = (req as any).userId as number;
  const me = await prisma.user.findUnique({
    where: { id: userId },
    select: { id: true, email: true, name: true, profile: true }
  });
  res.json(me);
});

r.put("/", async (req, res) => {
  const userId = (req as any).userId as number;
  const parsed = profileUpdateSchema.safeParse(req.body);
  if (!parsed.success) return res.status(400).json(parsed.error.format());
  const { displayName, bio } = parsed.data;
  const updated = await prisma.profile.upsert({
    where: { userId }, update: { displayName, bio }, create: { userId, displayName, bio }
  });
  res.json(updated);
});

export default r;
```

#### j) 파일: **server/src/routes.posts.ts**

```ts
import { Router } from "express";
import { prisma } from "./db";
import { authGuard } from "./auth";
import { postCreateSchema, postUpdateSchema } from "./validators";

const r = Router();

// 목록(공개)
r.get("/", async (req, res) => {
  const q = String(req.query.q ?? "");
  const where = q ? { OR: [{ title: { contains: q } }, { content: { contains: q } }] } : {};
  const posts = await prisma.post.findMany({
    where,
    orderBy: { createdAt: "desc" },
    include: { author: { select: { id: true, name: true, profile: true } } }
  });
  res.json(posts);
});

// 단건(공개)
r.get("/:id", async (req, res) => {
  const id = Number(req.params.id);
  const post = await prisma.post.findUnique({
    where: { id },
    include: {
      author: { select: { id: true, name: true, profile: true } },
      comments: {
        orderBy: { createdAt: "asc" },
        include: { author: { select: { id: true, name: true, profile: true } } }
      }
    }
  });
  if (!post) return res.status(404).json({ error: "not found" });
  res.json(post);
});

// 생성(보호)
r.post("/", authGuard, async (req, res) => {
  const parsed = postCreateSchema.safeParse(req.body);
  if (!parsed.success) return res.status(400).json(parsed.error.format());
  const userId = (req as any).userId as number;
  const created = await prisma.post.create({
    data: { ...parsed.data, authorId: userId }
  });
  res.status(201).json(created);
});

// 수정/삭제(보호, 본인만)
r.put("/:id", authGuard, async (req, res) => {
  const id = Number(req.params.id);
  const parsed = postUpdateSchema.safeParse(req.body);
  if (!parsed.success) return res.status(400).json(parsed.error.format());
  const userId = (req as any).userId as number;
  const post = await prisma.post.findUnique({ where: { id } });
  if (!post) return res.status(404).json({ error: "not found" });
  if (post.authorId !== userId) return res.status(403).json({ error: "forbidden" });
  const updated = await prisma.post.update({ where: { id }, data: parsed.data });
  res.json(updated);
});

r.delete("/:id", authGuard, async (req, res) => {
  const id = Number(req.params.id);
  const userId = (req as any).userId as number;
  const post = await prisma.post.findUnique({ where: { id } });
  if (!post) return res.status(404).json({ error: "not found" });
  if (post.authorId !== userId) return res.status(403).json({ error: "forbidden" });
  await prisma.post.delete({ where: { id } });
  res.status(204).end();
});

export default r;
```

#### k) 파일: **server/src/routes.comments.ts**

```ts
import { Router } from "express";
import { prisma } from "./db";
import { authGuard } from "./auth";
import { commentCreateSchema } from "./validators";

const r = Router();

// 댓글 생성(보호)
r.post("/:postId", authGuard, async (req, res) => {
  const postId = Number(req.params.postId);
  const parsed = commentCreateSchema.safeParse(req.body);
  if (!parsed.success) return res.status(400).json(parsed.error.format());
  const userId = (req as any).userId as number;
  const created = await prisma.comment.create({
    data: { content: parsed.data.content, postId, authorId: userId }
  });
  res.status(201).json(created);
});

// 댓글 삭제(보호, 본인만)
r.delete("/:commentId", authGuard, async (req, res) => {
  const id = Number(req.params.commentId);
  const userId = (req as any).userId as number;
  const c = await prisma.comment.findUnique({ where: { id } });
  if (!c) return res.status(404).json({ error: "not found" });
  if (c.authorId !== userId) return res.status(403).json({ error: "forbidden" });
  await prisma.comment.delete({ where: { id } });
  res.status(204).end();
});

export default r;
```

#### l) 파일: **server/src/index.ts** (부팅)

```ts
import express from "express";
import cors from "cors";
import cookieParser from "cookie-parser";
import authRouter from "./routes.auth";
import postsRouter from "./routes.posts";
import commentsRouter from "./routes.comments";
import profileRouter from "./routes.profile";

const app = express();
app.use(cors({ origin: "http://localhost:5173", credentials: true }));
app.use(express.json());
app.use(cookieParser());

app.get("/api/health", (_req, res) => res.json({ ok: true }));
app.use("/api/auth", authRouter);
app.use("/api/posts", postsRouter);
app.use("/api/comments", commentsRouter);
app.use("/api/profile", profileRouter);

const PORT = 5174;
app.listen(PORT, () => console.log(`API on http://localhost:${PORT}`));
```

#### m) **server/package.json (scripts)**

```json
{
  "scripts": {
    "dev": "ts-node-dev --respawn --transpile-only src/index.ts",
    "prisma:migrate": "prisma migrate dev",
    "prisma:generate": "prisma generate",
    "studio": "prisma studio"
  }
}
```

---

### 2-2) 프런트엔드 — **client/**

#### a) 초기 설정 + Tailwind

```bash
cd ../
npm create vite@latest client -- --template react-swc-ts
cd client
npm i
npm i react-router-dom
npm i -D tailwindcss postcss autoprefixer
npx tailwindcss init -p
```

#### b) 파일: **client/tailwind.config.js**

```js
/** @type {import('tailwindcss').Config} */
export default {
  content: [
    "./index.html",
    "./src/**/*.{ts,tsx}",
  ],
  theme: { extend: {} },
  plugins: [],
}
```

#### c) 파일: **client/src/index.css**

```css
@tailwind base;
@tailwind components;
@tailwind utilities;

:root { color-scheme: light dark; }
```

#### d) 파일: **client/vite.config.ts** (API 프록시)

```ts
import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react-swc'

export default defineConfig({
  plugins: [react()],
  server: {
    proxy: {
      '/api': 'http://localhost:5174'
    }
  }
})
```

#### e) 파일: **client/src/main.tsx**

```tsx
import { createRoot } from 'react-dom/client'
import { BrowserRouter, Routes, Route, Navigate } from 'react-router-dom'
import App from './App'
import './index.css'
import { AuthProvider } from './modules/auth/AuthContext'
import LoginPage from './pages/LoginPage'
import RegisterPage from './pages/RegisterPage'
import FeedPage from './pages/FeedPage'
import PostDetailPage from './pages/PostDetailPage'
import ProfilePage from './pages/ProfilePage'

createRoot(document.getElementById('root')!).render(
  <AuthProvider>
    <BrowserRouter>
      <Routes>
        <Route element={<App />}>
          <Route index element={<FeedPage />} />
          <Route path="login" element={<LoginPage />} />
          <Route path="register" element={<RegisterPage />} />
          <Route path="post/:id" element={<PostDetailPage />} />
          <Route path="profile" element={<ProfilePage />} />
          <Route path="*" element={<Navigate to="/" />} />
        </Route>
      </Routes>
    </BrowserRouter>
  </AuthProvider>
)
```

#### f) 파일: **client/src/App.tsx**

```tsx
import { Outlet, Link, useNavigate } from 'react-router-dom'
import { useAuth } from './modules/auth/AuthContext'

export default function App() {
  const { user, logout } = useAuth()
  const nav = useNavigate()
  return (
    <div className="min-h-screen bg-gray-50 text-gray-900">
      <header className="border-b bg-white">
        <div className="max-w-5xl mx-auto px-4 h-14 flex items-center gap-3">
          <Link to="/" className="font-semibold">Fullstack Board</Link>
          <nav className="ml-auto flex items-center gap-3">
            {user ? (
              <>
                <Link to="/profile" className="px-3 py-1 rounded bg-gray-100">프로필</Link>
                <button onClick={async()=>{ await logout(); nav('/login'); }}
                        className="px-3 py-1 rounded bg-black text-white">로그아웃</button>
              </>
            ) : (
              <>
                <Link to="/login" className="px-3 py-1 rounded bg-gray-100">로그인</Link>
                <Link to="/register" className="px-3 py-1 rounded bg-black text-white">회원가입</Link>
              </>
            )}
          </nav>
        </div>
      </header>
      <main className="max-w-5xl mx-auto px-4 py-6">
        <Outlet />
      </main>
    </div>
  )
}
```

#### g) 파일: **client/src/modules/auth/AuthContext.tsx**

```tsx
import React, { createContext, useContext, useEffect, useState } from "react";

type User = { id: number; email: string; name: string };
type Ctx = {
  user: User | null;
  accessToken: string | null;
  login: (email: string, password: string) => Promise<void>;
  register: (email: string, name: string, password: string) => Promise<void>;
  logout: () => Promise<void>;
  api: <T=any>(input: RequestInfo, init?: RequestInit) => Promise<T>;
};
const C = createContext<Ctx>(null as any);
export const useAuth = () => useContext(C);

export function AuthProvider({ children }: { children: React.ReactNode }) {
  const [user, setUser] = useState<User | null>(null);
  const [accessToken, setAccessToken] = useState<string | null>(null);

  async function login(email: string, password: string) {
    const r = await fetch("/api/auth/login", {
      method: "POST", credentials: "include",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify({ email, password })
    });
    if (!r.ok) throw new Error("로그인 실패");
    const d = await r.json();
    setAccessToken(d.accessToken); setUser(d.user);
  }

  async function register(email: string, name: string, password: string) {
    const r = await fetch("/api/auth/register", {
      method: "POST", credentials: "include",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify({ email, name, password })
    });
    if (!r.ok) throw new Error("회원가입 실패");
    const d = await r.json();
    setAccessToken(d.accessToken); setUser(d.user);
  }

  async function logout() {
    await fetch("/api/auth/logout", { method: "POST", credentials: "include" });
    setUser(null); setAccessToken(null);
  }

  async function refresh() {
    const r = await fetch("/api/auth/refresh", { method: "POST", credentials: "include" });
    if (!r.ok) throw new Error("리프레시 실패");
    const d = await r.json(); setAccessToken(d.accessToken); return d.accessToken as string;
  }

  const api = async <T,>(input: RequestInfo, init: RequestInit = {}) => {
    const run = (token?: string) =>
      fetch(input, { ...init, headers: { ...(init.headers||{}), ...(token ? { Authorization: `Bearer ${token}` } : {}) } });
    let res = await run(accessToken || undefined);
    if (res.status === 401) { const t = await refresh(); res = await run(t); }
    if (!res.ok) throw new Error(`API ${res.status}`); return res.json() as Promise<T>;
  };

  // 새로고침 시 세션 복구
  useEffect(()=>{ (async()=>{
    try { const t = await refresh(); await api("/api/profile/me",{method:"GET"}); setAccessToken(t); }
    catch(_) {}
  })(); // eslint-disable-next-line
  },[]);

  return <C.Provider value={{ user, accessToken, login, register, logout, api }}>{children}</C.Provider>
}
```

#### h) 파일: **client/src/pages/LoginPage.tsx**

```tsx
import { FormEvent, useState } from "react";
import { useAuth } from "../modules/auth/AuthContext";
import { useNavigate, Link } from "react-router-dom";

export default function LoginPage() {
  const { login } = useAuth();
  const nav = useNavigate();
  const [email, setEmail] = useState("user@example.com");
  const [password, setPassword] = useState("1234");
  const [err, setErr] = useState<string | null>(null);

  async function onSubmit(e: FormEvent) {
    e.preventDefault();
    try { await login(email, password); nav("/"); }
    catch (e:any) { setErr(e.message ?? "로그인 실패"); }
  }

  return (
    <div className="max-w-md mx-auto">
      <h1 className="text-xl font-semibold mb-3">로그인</h1>
      <form onSubmit={onSubmit} className="bg-white p-4 rounded-xl shadow grid gap-3">
        <input className="border rounded px-3 py-2" placeholder="email" value={email} onChange={e=>setEmail(e.target.value)} />
        <input className="border rounded px-3 py-2" type="password" placeholder="password" value={password} onChange={e=>setPassword(e.target.value)} />
        {err && <p className="text-red-600 text-sm">{err}</p>}
        <button className="px-3 py-2 rounded bg-black text-white">로그인</button>
        <p className="text-sm text-gray-500">계정이 없나요? <Link className="underline" to="/register">회원가입</Link></p>
      </form>
    </div>
  );
}
```

#### i) 파일: **client/src/pages/RegisterPage.tsx**

```tsx
import { FormEvent, useState } from "react";
import { useAuth } from "../modules/auth/AuthContext";
import { useNavigate } from "react-router-dom";

export default function RegisterPage() {
  const { register } = useAuth();
  const nav = useNavigate();
  const [email, setEmail] = useState("user@example.com");
  const [name, setName] = useState("데모유저");
  const [password, setPassword] = useState("1234");
  const [err, setErr] = useState<string | null>(null);

  async function onSubmit(e: FormEvent) {
    e.preventDefault();
    try { await register(email, name, password); nav("/"); }
    catch (e:any) { setErr(e.message ?? "회원가입 실패"); }
  }

  return (
    <div className="max-w-md mx-auto">
      <h1 className="text-xl font-semibold mb-3">회원가입</h1>
      <form onSubmit={onSubmit} className="bg-white p-4 rounded-xl shadow grid gap-3">
        <input className="border rounded px-3 py-2" placeholder="email" value={email} onChange={e=>setEmail(e.target.value)} />
        <input className="border rounded px-3 py-2" placeholder="name" value={name} onChange={e=>setName(e.target.value)} />
        <input className="border rounded px-3 py-2" type="password" placeholder="password" value={password} onChange={e=>setPassword(e.target.value)} />
        {err && <p className="text-red-600 text-sm">{err}</p>}
        <button className="px-3 py-2 rounded bg-black text-white">가입</button>
      </form>
    </div>
  );
}
```

#### j) 파일: **client/src/pages/FeedPage.tsx**

```tsx
import { FormEvent, useEffect, useState } from "react";
import { useAuth } from "../modules/auth/AuthContext";
import { Link } from "react-router-dom";

type Post = {
  id: number; title: string; content: string; createdAt: string;
  author: { id: number; name: string; profile?: { displayName: string } | null }
};

export default function FeedPage() {
  const { user, api } = useAuth();
  const [items, setItems] = useState<Post[]>([]);
  const [q, setQ] = useState("");
  const [title, setTitle] = useState(""); const [content, setContent] = useState("");

  async function load() {
    const r = await fetch(`/api/posts?q=${encodeURIComponent(q)}`);
    setItems(await r.json());
  }
  useEffect(()=>{ load(); }, [q]);

  async function onCreate(e: FormEvent) {
    e.preventDefault();
    if (!title.trim() || !content.trim()) return;
    await api("/api/posts", { method:"POST", headers:{ "Content-Type":"application/json" }, body: JSON.stringify({ title, content }) });
    setTitle(""); setContent(""); await load();
  }

  return (
    <div className="grid gap-4">
      <div className="flex items-center gap-2">
        <input placeholder="검색(제목/내용)" value={q} onChange={e=>setQ(e.target.value)}
               className="border rounded px-3 py-2 w-full"/>
      </div>

      {user && (
        <form onSubmit={onCreate} className="bg-white p-4 rounded-xl shadow grid gap-2">
          <strong>새 글 작성</strong>
          <input className="border rounded px-3 py-2" placeholder="제목" value={title} onChange={e=>setTitle(e.target.value)} />
          <textarea className="border rounded px-3 py-2" rows={3} placeholder="내용" value={content} onChange={e=>setContent(e.target.value)} />
          <button className="px-3 py-2 rounded bg-black text-white self-start">등록</button>
        </form>
      )}

      <ul className="grid gap-3">
        {items.map(p=>(
          <li key={p.id} className="bg-white p-4 rounded-xl shadow">
            <div className="flex items-baseline gap-2">
              <h3 className="font-semibold text-lg">{p.title}</h3>
              <span className="text-gray-500 text-sm">{new Date(p.createdAt).toLocaleString()}</span>
              <span className="ml-auto text-sm text-gray-600">
                by {p.author.profile?.displayName ?? p.author.name}
              </span>
            </div>
            <p className="mt-2 text-gray-800 whitespace-pre-wrap">{p.content}</p>
            <Link to={`/post/${p.id}`} className="underline text-sm mt-2 inline-block">자세히 보기 →</Link>
          </li>
        ))}
      </ul>
    </div>
  );
}
```

#### k) 파일: **client/src/pages/PostDetailPage.tsx**

```tsx
import { useEffect, useState } from "react";
import { useParams, useNavigate } from "react-router-dom";
import { useAuth } from "../modules/auth/AuthContext";

type Comment = {
  id: number; content: string; createdAt: string;
  author: { id:number; name:string; profile?:{displayName:string}|null }
};
type Post = {
  id:number; title:string; content:string; createdAt:string; authorId:number;
  author:{ id:number; name:string; profile?:{displayName:string}|null };
  comments: Comment[];
};

export default function PostDetailPage() {
  const { id="" } = useParams();
  const { user, api } = useAuth();
  const nav = useNavigate();
  const [post, setPost] = useState<Post | null>(null);
  const [comment, setComment] = useState("");

  async function load() {
    const r = await fetch(`/api/posts/${id}`);
    if (r.status === 404) return nav("/");
    setPost(await r.json());
  }
  useEffect(()=>{ load(); }, [id]);

  async function addComment() {
    if (!comment.trim()) return;
    await api(`/api/comments/${id}`, { method:"POST", headers:{ "Content-Type":"application/json" }, body: JSON.stringify({ content: comment }) });
    setComment(""); await load();
  }

  async function removePost() {
    if (!confirm("삭제하시겠어요?")) return;
    await api(`/api/posts/${id}`, { method:"DELETE" });
    nav("/");
  }

  return post ? (
    <div className="grid gap-4">
      <div className="bg-white p-4 rounded-xl shadow">
        <div className="flex items-baseline gap-2">
          <h1 className="text-xl font-semibold">{post.title}</h1>
          <span className="text-gray-500 text-sm">{new Date(post.createdAt).toLocaleString()}</span>
          <span className="ml-auto text-sm text-gray-600">
            by {post.author.profile?.displayName ?? post.author.name}
          </span>
        </div>
        <p className="mt-3 whitespace-pre-wrap">{post.content}</p>
        {user?.id === post.authorId && (
          <div className="mt-3 flex gap-2">
            <button onClick={removePost} className="px-3 py-1 rounded bg-red-600 text-white">글 삭제</button>
          </div>
        )}
      </div>

      <div className="bg-white p-4 rounded-xl shadow grid gap-3">
        <strong>댓글 ({post.comments.length})</strong>
        {user && (
          <div className="flex gap-2">
            <input className="border rounded px-3 py-2 w-full" placeholder="댓글 입력"
                   value={comment} onChange={e=>setComment(e.target.value)} />
            <button onClick={addComment} className="px-3 py-2 rounded bg-black text-white">등록</button>
          </div>
        )}
        <ul className="grid gap-2">
          {post.comments.map(c=>(
            <li key={c.id} className="border rounded-lg px-3 py-2">
              <div className="text-sm text-gray-600 flex gap-2">
                <span>{c.author.profile?.displayName ?? c.author.name}</span>
                <span>· {new Date(c.createdAt).toLocaleString()}</span>
              </div>
              <p className="mt-1">{c.content}</p>
            </li>
          ))}
        </ul>
      </div>
    </div>
  ) : <p>불러오는 중…</p>;
}
```

#### l) 파일: **client/src/pages/ProfilePage.tsx**

```tsx
import { FormEvent, useEffect, useState } from "react";
import { useAuth } from "../modules/auth/AuthContext";

export default function ProfilePage() {
  const { api } = useAuth();
  const [displayName, setDisplayName] = useState("");
  const [bio, setBio] = useState("");

  useEffect(()=>{ (async()=>{
    const me = await api<{profile?:{displayName:string; bio:string}} & {name:string; email:string}>("/api/profile/me",{method:"GET"});
    setDisplayName(me.profile?.displayName ?? me.name);
    setBio(me.profile?.bio ?? "");
  })(); // eslint-disable-next-line
  },[]);

  async function onSave(e: FormEvent) {
    e.preventDefault();
    await api("/api/profile", {
      method:"PUT", headers:{ "Content-Type":"application/json" },
      body: JSON.stringify({ displayName, bio })
    });
    alert("저장되었습니다.");
  }

  return (
    <form onSubmit={onSave} className="max-w-lg bg-white p-4 rounded-xl shadow grid gap-3">
      <h1 className="text-xl font-semibold">프로필</h1>
      <input className="border rounded px-3 py-2" placeholder="표시 이름"
             value={displayName} onChange={e=>setDisplayName(e.target.value)} />
      <textarea className="border rounded px-3 py-2" rows={4} placeholder="소개"
                value={bio} onChange={e=>setBio(e.target.value)} />
      <button className="px-3 py-2 rounded bg-black text-white w-fit">저장</button>
    </form>
  );
}
```

---

## 3) 실행 방법

터미널 1 — **백엔드**:

```bash
cd fullstack-board/server
# .env에 ACCESS_SECRET/REFRESH_SECRET 설정 확인
npx prisma migrate dev
npm run dev
```

터미널 2 — **프런트**:

```bash
cd fullstack-board/client
npm run dev
```

브라우저: **[http://localhost:5173](http://localhost:5173)**

1. 회원가입 → 2) 로그인 → 3) 글 작성 → 4) 상세에서 댓글 작성 → 5) 프로필 수정

---

## 4) 실습 미션 (팀 발표용 체크리스트)

1. **수정/삭제 UI 완성**

   * 글 수정(제목/내용)·댓글 삭제 버튼 추가 및 본인 소유만 노출.

2. **이미지 아바타(선택)**

   * 프로필에 아바타 URL 필드 추가 후 표시. (Prisma 모델/페이지 수정)

3. **페이지네이션**

   * 목록에 `?cursor` 기반 더보기(무한스크롤) 추가.

4. **권한 표시**

   * 게시글 카드에 “내 글” 뱃지. 삭제/수정 버튼 접근 제어.

5. **디자인 업그레이드**

   * Tailwind 컴포넌트 유틸(예: daisyUI) 도입 또는 MUI로 전환 비교.

6. **Postgres 전환(발표 과제)**

   * `schema.prisma`의 provider/url 변경 → `prisma migrate dev`
   * 차이점/문제 해결 기록 후 공유.

---

## 5) 자주 하는 실수 & 베스트 프랙티스

* **토큰 저장 위치**: 액세스 토큰은 **메모리**에, 리프레시는 **HttpOnly 쿠키**(서버 전용).
* **권한 체크 누락**: 서버에서 **작성자 검사** 필수(클라 제어는 우회 가능).
* **에러 메시지**: Zod 에러를 그대로 노출하지 말고 프런트에서 보기 좋게 가공.
* **Prisma 마이그레이션**: 모든 스키마 변경은 `prisma migrate`로 추적.
* **시간/로케일**: DB는 UTC 저장, 클라이언트에서 지역 시간 표기.

---

## 6) 예상 질문(Q\&A)

**Q1. Next.js로도 같은 걸 만들 수 있나요?**
A. 가능합니다. App Router에서 **Route Handlers**(`/app/api/**/route.ts`)로 API를 만들고, 페이지는 **서버/클라이언트 컴포넌트 혼합**으로 구현하면 됩니다. 구조는 동일합니다.

**Q2. 보안 강화를 하려면?**
A. HTTPS + `secure` 쿠키, 리프레시 토큰 **회전/폐기**, 레이트 리미트, CORS 도메인 고정, Helmet, 감사 로그를 추가하세요.

**Q3. 대규모 트래픽이면?**
A. DB 커넥션 풀링, 캐시(게시글 조회), read replica, 페이징 최적화, 인덱스 튜닝을 검토합니다.

**Q4. 상태 관리 라이브러리는 필요 없나요?**
A. 본 실습은 Context만 사용했습니다. 규모가 커지면 **Zustand/RTK**로 전역 상태를 분리해 유지보수성을 높이세요.


